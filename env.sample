export API_HOST=localhost
export PORT=2015
export POSTGRE_CONNECTION_STRING=postgresql://localhost:5432/skittles

## log level - given a chosen level, log events only at or above that level
## debug < info < warn < error < exception < stat
export LOG_LEVEL=info

## Log to file - LOG_FILE_PATH and LOG_FILENAME must be defined
## ERROR_FILENAME is optional
# export LOG_FILE_PATH=/home/cade/code/api.webmaker.org/
# export LOG_FILENAME=api.log
# export ERROR_FILENAME=error.log
